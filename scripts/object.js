class rect{
    // the x,y value of the spec is the center of the object
    constructor(spec){
        this.height = spec.height;
        this.width = spec.width;
        this.x = spec.x;
        this.y = spec.y;
    }
}